import { CalculatingNode } from '../../../types.ts'

export const buildGradientTree = (calculatingHistory: CalculatingNode) => {
  // Convert calculatingHistory to tree for gradient
  const gradientTree = calculatingHistory
  return gradientTree
}